{
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/img/(.*)",
            "dest": "/src"
        },
        {
            "src": "/api/(.*)",
            "dest": "server.js"
        }
    ]
}

{
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/img/:match*",
            "dest": "/src"
        },
        {
            "src": "/api/(.*)",
            "dest": "server.js"
        }
    ]
}
